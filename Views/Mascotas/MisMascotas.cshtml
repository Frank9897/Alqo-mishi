@model IEnumerable<AlqoMishi.Entidades.Mascota>

<h2>Mis mascotas</h2>

<a class="btn btn-primary mb-3" asp-action="Crear">Registrar mascota</a>

<table class="table table-striped">

<thead>
<tr>
<th>Nombre</th>
<th>Especie</th>
<th>Raza</th>
<th>Edad</th>
<th>Sexo</th>
<th></th>
</tr>
</thead>

<tbody>

@foreach(var mascota in Model)
{
<tr>

<td>@mascota.Nombre</td>
<td>@mascota.Especie</td>
<td>@mascota.Raza</td>
<td>@mascota.Edad</td>
<td>@mascota.Sexo</td>
<td>

<a asp-action="Editar"
   asp-route-id="@mascota.Id"
   class="btn btn-warning btn-sm">
Editar
</a>

<a asp-action="Eliminar"
   asp-route-id="@mascota.Id"
   class="btn btn-danger btn-sm"
   onclick="return confirm('¿Seguro que deseas eliminar esta mascota?')">
Eliminar
</a>

<a asp-controller="Admin"
   asp-action="Historial"
   asp-route-mascotaId="@mascota.Id"
   class="btn btn-outline-primary btn-sm">
Historial
</a>

</td>
</tr>
}

</tbody>

</table>